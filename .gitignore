# --- secrets / credentials (never commit) ---
.env
config/users.secrets.json

# --- python virtualenv ---
.venv/

# --- data, outputs, run state (not version-controlled) ---
/annotation/
/publikationsbot_output/
/querygen/
/logs/
*.log
.pipeline.lock

# --- python caches ---
__pycache__/
*.py[cod]
